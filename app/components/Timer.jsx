var React = require('react');

var Timer = React.createClass({
    render: function() {
        return (
            <div>
                Timer.jsx renderd
            </div>
        );
    }
});

module.exports = Timer;